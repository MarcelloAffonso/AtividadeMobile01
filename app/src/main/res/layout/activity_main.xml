<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:id="@+id/paisesLinearLayout"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:orientation="vertical">

    <TextView
        android:id="@+id/paisTextView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:text="@string/pais_local"
        android:textSize="@dimen/dimenTextSize"
        android:gravity="center"
        android:layout_weight="1"/>

    <ImageView
        android:id="@+id/paisImageView"
        android:layout_width="@dimen/dimenImageView"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:layout_gravity="center"
        android:contentDescription="@string/pais_local"
        app:srcCompat="@drawable/bandeira" />

    <ImageView
        android:id="@+id/comidaImageView"
        android:layout_width="@dimen/dimenImageView"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:layout_gravity="center"
        android:contentDescription="@string/comida_local"
        app:srcCompat="@drawable/comida" />

    <TextView
        android:id="@+id/comidaTextView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:textSize="@dimen/dimenTextSize"
        android:gravity="center"
        android:text="@string/comida_local" />

</LinearLayout>